# Pulse 01: Shape metadata contracts

## Intent

Expose explicit invariant and extension metadata through the derive macro and
retain a small shape calculator descriptor collection.

## Validation

```powershell
cargo test -p rune-derive
cargo test -p rune-shape-calculator
```
