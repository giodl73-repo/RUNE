# Pulse 01: Core and derive metadata

## Intent

Add field metadata records to RUNE core and author them through
`#[rune_field(...)]`.

## Validation

```powershell
cargo test -p rune-core
cargo test -p rune-derive
```
